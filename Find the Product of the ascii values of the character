#include<iostream>
using namespace std;

long long productAscii(string str)
{
    long long prod = 1;
    for(int i = 0; i < str.length(); i++)
    {
        prod *= (int)str[i];
    }
    return prod;
}

int main()
{
    string str = "GfG";
 
    cout << productAscii(str);
 
    return 0;
}
