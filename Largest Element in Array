class Solution
{
public:
    int largest(vector<int> &arr, int n)
    {
        int LargeElement = arr[0];
        for(int i = 0; i < n; i++)
        {
            LargeElement = max(LargeElement, arr[i]);
        }
        return LargeElement;
    }
};
